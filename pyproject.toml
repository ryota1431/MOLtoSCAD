[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "moltoscad"
version = "0.1.0"
description = "Utilities to convert MOL files to OpenSCAD format"
authors = [{name = "ryota1431"}]
readme = "readme.md"
requires-python = ">=3.8"
dependencies = ["rdkit"]

